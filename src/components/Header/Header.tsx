import { StyledHeader, StyledInput } from "./StyledHeader"
export const Header = () => {
    return (
        <StyledHeader>
            <StyledInput placeholder="Search for task..." />
        </StyledHeader>
    )
}